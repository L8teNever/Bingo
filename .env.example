# Umgebungsvariablen für Dinner Bingo

# WICHTIG: Ändere diese Werte für Production!
SECRET_KEY=your-secret-key-change-in-production

# Flask-Umgebung (development oder production)
FLASK_ENV=production

# Optional: Datenbank-URL (Standard: SQLite im instance-Ordner)
# DATABASE_URL=sqlite:///instance/bingo.db
# Für PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost/bingo
