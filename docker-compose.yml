version: '3.8'

services:
  bingo:
    image: ${DOCKER_USERNAME:-yourusername}/bingo:latest
    container_name: dinner_bingo
    ports:
      - "5000:5000"
    volumes:
      - ./bingo.db:/app/bingo.db
    environment:
      - SECRET_KEY=${SECRET_KEY:-bingo-secret-key-123}
    restart: always
